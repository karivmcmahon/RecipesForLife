CREATE TABLE Users (id int IDENTITY NOT NULL, updateTime datetime NOT NULL, firstName varchar(255) NOT NULL, secondName varchar(255) NOT NULL, cookingInterest varchar(255) NULL, bio varchar(255) NULL, city varchar(255) NULL, country varchar(255) NOT NULL, PRIMARY KEY (id));
